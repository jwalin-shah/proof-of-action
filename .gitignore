# Private zone — NEVER commit
private/
artifacts/private_*.md
artifacts/*.log
artifacts/runtime/
artifacts/human_review/
.env
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
uv.lock
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# OS
.DS_Store
.env.local

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf

# Per-user Gmail OAuth token — lives on the operator's Mac, never committed.
.config/

# credentials.json — operator's real GCP OAuth client. Never committed.
# credentials.example.json is the tracked template.
credentials.json
